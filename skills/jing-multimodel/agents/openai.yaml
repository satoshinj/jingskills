interface:
  display_name: "Jing Multimodel"
  short_description: "多模型分工验收，并用 Grok 实时调研 X、Reddit 和网页"
  default_prompt: "Use $jing-multimodel to route this task, run isolated Grok research when needed, and verify the final result."
